<% grant = Hyrax::GrantService.new %>

<%= f.input :grant, as: :select,
    collection: grant.select_active_options,
    include_blank: true, required: false,
    item_helper: grant.method(:include_current_value),
    input_html: { class: 'form-control' } %>